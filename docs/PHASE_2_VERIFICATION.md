# Phase 2 Verification

| Feature | Implemented? | Files Changed | How to Test | Remarks |
|---------|--------------|---------------|-------------|---------|
| Company Profile | Yes | `app/models/settings.py`, `app/routes/settings_routes.py`, `app/templates/settings/company.html`, `app/services/numbering_service.py`, `seed.py` | Log in as admin, open Settings > Company Profile, edit legal/trade name, GSTIN, currency, financial year month, invoice prefix, and save. | Saves to `companies` and syncs legacy `company_settings` for existing invoice numbering/templates. |
| Branches / Business Locations | Yes | `app/models/settings.py`, `app/routes/settings_routes.py`, `app/templates/settings/branches.html`, `app/templates/settings/branch_form.html`, `seed.py` | Open Settings > Branches, add a branch, search/filter it, edit it, then deactivate/reactivate. | Uses active/inactive status; no hard delete. |
| Warehouses / Storage Locations | Yes | `app/models/product.py`, `app/routes/settings_routes.py`, `app/routes/master_routes.py`, `app/templates/settings/warehouses.html`, `app/templates/settings/warehouse_form.html`, `seed.py` | Open Settings > Warehouses, add/edit a warehouse linked to a branch, then deactivate/reactivate. | Legacy Masters > Warehouses now redirects to Settings > Warehouses. |
| POS Registers / Counters | Yes | `app/models/settings.py`, `app/models/sales.py`, `app/routes/settings_routes.py`, `app/routes/pos_routes.py`, `app/templates/settings/registers.html`, `app/templates/settings/register_form.html`, `app/templates/pos/open_session.html`, `seed.py` | Open Settings > POS Registers, create a register linked to a branch and warehouse, then open POS and select that register. | Includes receipt printer placeholder and stores `POSSession.register_id`. |
| Users | Yes | `app/models/user.py`, `app/routes/settings_routes.py`, `app/templates/settings/users.html`, `app/templates/settings/user_form.html`, `seed.py` | Open Settings > Users, create a user, assign a role, edit details, deactivate/reactivate. | User status uses existing `User.is_active`; current user cannot deactivate self. |
| Roles & Permissions | Yes | `app/models/user.py`, `app/routes/settings_routes.py`, `app/templates/settings/roles.html`, `app/templates/settings/role_permissions.html`, `app/__init__.py`, `seed.py` | Open Settings > Roles & Permissions, create/edit a role, open Matrix, grant/revoke permissions, then test access with that role. | Route access is enforced globally by blueprint/module and action. |
| Permission Matrix Modules | Yes | `seed.py`, `app/__init__.py` | Run `python seed.py`, then inspect Settings > Roles & Permissions. | Seeds requested Admin, Manager, Cashier, Inventory Staff, Accountant roles and Products, Inventory, POS, Sales, Purchases, Reports, Settings modules. |
| Audit Log Foundation | Yes | `app/services/audit_service.py`, `app/routes/settings_routes.py`, `app/routes/stock_routes.py`, `app/routes/invoice_routes.py`, `app/routes/accounts_routes.py`, `app/routes/auth_routes.py` | Perform login/logout, update company/branch/warehouse/register/user/role, make stock adjustment, cancel invoice, or record payment; view Reports > Activity Log. | Login/logout existed; this phase adds business setup, stock adjustment, invoice cancellation, and payment update audit entries. |
| Navigation | Yes | `app/templates/base.html` | Log in and verify Settings links for Company Profile, Branches, Warehouses, Registers, Users, Roles & Permissions, and Reports > Activity Log. | POS remains prominent in the sidebar/top bar. |
| Database Migration & Seed Defaults | Yes | `migrations/versions/2e57f7f58e07_business_setup_foundation.py`, `seed.py` | Run `flask db upgrade` and `python seed.py`. | Migration is idempotent for existing projects where tables may have been created by startup schema checks. |
